version: '3'
services:
  nextjs:
    container_name: nextjs
    build:
      context: ./
      dockerfile: Dockerfile
  nginx:
    container_name: nginx
    build:
      context: ./nginx
      dockerfile: Dockerfile
    ports:
      - 80:80
    depends_on:
      - nextjs
